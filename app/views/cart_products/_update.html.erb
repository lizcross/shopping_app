<%= form_for(update) do |f| %>
	<div class="row">
	    <div class="col-md-6"><%= f.number_field :quantity, class: 'form-control' %></div>
		<div class="col-md-6"><%= f.submit "Update", class: "btn" %></div>
	</div>
<% end %>