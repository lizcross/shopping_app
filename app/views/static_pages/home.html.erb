<div id="listing">
  <% for product in @products %>
    <div class="product">
      <%= link_to image_tag(product.image, width: "120", height: "172"), product %><br/>
      <div style="word-wrap: break-word; width: 120px" ><%= link_to product.name, product %></div>
    </div>
  <% end %>
</div>
