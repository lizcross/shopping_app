<div id="listing">
  <% for product in @products %>
    <div class="product">
      <%= link_to image_tag(product.image, width: "120", height: "172"), product %><br/>
      <%= link_to product.name, product %>
    </div>
  <% end %>
</div>
